package com.example.UserPackage


class Computer{
/*
  override def receive: Receive = {
    case Server.MoveRequest(board: Game) =>
      val decisionTree = new DecisionTree(board)
      decisionTree.createDecisionTree()
      Thread.sleep(500)
      sender ! Server.MoveReceived(decisionTree.findBestMove())

 */

}